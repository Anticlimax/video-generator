# ambient-video-generator

MVP for generating longform ambient music videos from a theme using OpenClaw, local media tooling, and procedural video templates.

Current provider stance:

- `mock/local` is the default working path
- external audio providers are pluggable
- `infsh` is optional and not the default source of music generation
- the current first real provider direction is `ElevenLabs Music API`
